# Leave this empty during local development so Vite's /api and /media proxy is used.
VITE_API_BASE_URL=

# In production, set the public backend origin, for example:
# VITE_API_BASE_URL=https://api.example.com
